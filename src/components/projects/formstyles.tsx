import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  input: {
    marginTop: "20px",
    marginLeft: "auto",
    marginRight: "auto",
    width: "500px",
    display: "block",
  },
});
